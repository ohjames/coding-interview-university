def countSetBits(n):
    count = 0
    while n:
        count += n & 1
        n >>= 1
    return count 

#Recursion
def countSetBits(n):
    if n == 0:
        return 0
    else:
        return (n & 1) + countSetBits(n >> 1)

int.bit_count()

mask = x >> 31 # shift to right by 31bits; 1s for negative and 0 for positive 
x = x XOR mask # get those that are both
x = x - mask   # +1 for negatives and +0 for positives